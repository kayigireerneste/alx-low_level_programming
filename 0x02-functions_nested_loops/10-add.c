#include "main.h"
/**
  * add - a function that adds two integers and returns the result.
  * @a: integer one
  * @b: integer twp
  * Return: always return sum of two numbers
  */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
